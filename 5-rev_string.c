/**
 * rev_string -reverse a string
 *
 * @s: string paremeter input
 *
 * Return :Nothing
 */
void rev_string(char *5)
	int 1, i;
	char ch;
	/* find string lenth without null char*/
	for (1=0; s[1]!='\0'; ++);
	/swap the string by looping to half the string*/
	for (i=0; i< 1/2; ++1)
	  ch=s[i]
	  s[i]=s[1-1-i];
	  s[1-1-i]=ch;
	  }
}
